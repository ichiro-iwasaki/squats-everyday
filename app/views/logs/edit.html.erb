<div>
  回数を編集する
</div>

<%= form_for("/logs/#{@log.id}", method: :patch) do |f| %>

  <%= f.label :number, "回数を入力" %>
  <%= f.number_field :number, class: 'form-control' %>
  <%= f.submit "保存", class: "btn btn-primary" %>

<% end %>